{
  "name": "blockguard",
  "version": "0.0.1",
  "scripts": {
    "build": "cd frontend && npm run build",
    "open": "npm run build && npx cap sync && npx cap open android",
    "refresh": "npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.3.0",
    "@capacitor/cli": "^5.3.0",
    "@capacitor/core": "^5.3.0",
    "@capacitor/geolocation": "^5.0.6",
    "@capacitor/ios": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
